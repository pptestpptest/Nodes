<fileHeader>

#warning("Manually move this test file to the corresponding test target.")

import <testImport1>
import <testImport2>

final class RootViewControllerTests: XCTestCase {

    func testNotNil() {
        XCTAssertNotNil(givenViewController(with: RootViewState()))
    }

    private func givenViewController(
        with state: RootViewState
    ) -> RootViewController {
        RootViewController(
            initialState: state,
            <viewStatePropertyName>: Empty().eraseToAnyPublisher()
        )
    }
}
